{
	"name": "suggestify-engine",
	"description": "Suggestions engine for Suggestify",
	"version": "1.2.2",
	"author": "Max van der Schee",
	"license": "MIT",
	"private": true,
	"scripts": {
		"pre:build": "node -e 'require(`./src/pre-build`).init()'",
		"build": "vite build && cd api && yarn"
	},
	"devDependencies": {
		"vite": "^2.4.4"
	}
}
